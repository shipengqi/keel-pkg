apiVersion: v1
kind: Config
clusters:
  - cluster:
      certificate-authority: {HOME_DIR}/ssl/ca.crt
      server: https://{K8S_APISERVER_IP}:{MASTER_API_SSL_PORT}
    name: keel-kubernetes
contexts:
  - context:
      cluster: keel-kubernetes
      user: {USER}
    name: {USER_CREDENTIAL}
current-context: {USER_CREDENTIAL}
users:
  - name: {USER}
    user:
      client-certificate: {HOME_DIR}/ssl/{USER_CREDENTIAL}.crt
      client-key: {HOME_DIR}/ssl/{USER_CREDENTIAL}.key
